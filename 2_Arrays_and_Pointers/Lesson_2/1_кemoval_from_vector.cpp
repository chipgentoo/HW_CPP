#include <iostream>
#include <vector>

int main()
{
    int vecSize;
    std::cout << "Enter number of value from Vector: ";
    std::cin >> vecSize;

    std::vector<int> intVector(vecSize);

    
    return 0;
}
